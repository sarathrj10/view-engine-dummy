
<h4>Items as List</h4>
<div class="row ml-3">
    <dl class="list-group">
        {{#each items}}
        <dt class="mt-3">{{this.name}}</dt>
        <dd>Price: {{this.price}}</dd>
        <dd>Quantity: {{this.quantity}}</dd>
        {{/each}}
    </dl>
</div>

<a href="/"><button type="button" class="btn btn-primary">View as Table</button></a>
<a href="/cards"><button type="button" class="btn btn-primary">View as Cards</button></a>